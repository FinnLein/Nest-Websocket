services: 
  postgres: 
    image: postgres:16-alpine
    container_name: nest-websocket-postgres
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASS}
      POSTGRES_DB: ${DATABASE_DB}
    ports: 
      - '5433:5432'
    volumes:
      - pg_data:/data

volumes: 
  pg_data: